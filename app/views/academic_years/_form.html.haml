= form_for @academic_year do |f|
  - if @academic_year.errors.any?
    #error_explanation
      %h2= "#{pluralize(@academic_year.errors.count, "error")} prohibited this academic_year from being saved:"
      %ul
        - @academic_year.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :year
    = f.text_field :year
  .actions
    = f.submit 'Save'
